# Inject Indexes Extension

---

## About this extension

This extension automatically injects two indexes in the transfer_process an contract_negotiation tables. Yes, it could have been done with migrations, but we didn't want to live with them forever since this is a fix and the actual migrations will be included in a later version (0.7.X).